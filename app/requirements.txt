# FastAPI and ASGI server
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.34.0,<1.0.0
gunicorn==21.2.0
websockets>=15.0.1,<16.0.0

# Task queue
celery==5.3.6
redis==5.0.1

# Async Redis for WebSocket
redis[hiredis]>=5.0.1

# Templating
jinja2>=3.1.3,<4.0.0

# Environment and HTTP
python-dotenv==1.0.0
requests>=2.32.4,<3.0.0

# Structured logging
structlog==24.1.0
python-json-logger==2.0.7

# Prometheus metrics
prometheus-client==0.19.0
prometheus-fastapi-instrumentator==6.1.0

# Pydantic (explicit v2)
pydantic>=2.5.3,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# Async support
httpx>=0.27.0
aiofiles==23.2.1

# Date/time handling
python-dateutil>=2.9.0

# PDF Generation
reportlab==4.1.0

# Celery Beat for scheduled tasks
celery[redis]==5.3.6

# Docker SDK for autoscaler
docker==7.0.0

# Kombu for advanced queue configuration
kombu==5.3.4

# Flower for Celery monitoring
flower==2.0.1

# JWT Authentication
PyJWT==2.8.0

# Password hashing
bcrypt==4.1.2

# AI/ML Triage (optional — gracefully disabled if no provider configured)
# Supports Anthropic Claude (cloud), Google ADK + LiteLLM (local vLLM/Qwen),
# or any OpenAI-compatible endpoint.
anthropic>=0.39.0
openai>=1.50.0
google-adk==1.18.0
litellm>=1.83.0


# Testing
pytest==8.0.0
pytest-asyncio==0.23.8
httpx>=0.27.0
fakeredis==2.21.0

# Required for OIDC RS256 ID-token validation (PyJWT asymmetric algorithms)
cryptography==42.0.8

# Postgres (Phase 1 datastore migration) — async SQLAlchemy + driver + migrations
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0
psycopg[binary]==3.2.3
